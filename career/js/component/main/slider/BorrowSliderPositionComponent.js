import { BaseMainSliderPositionComponent } from "./BaseMainSliderPositionComponent.js";

export class BorrowSliderPositionComponent extends BaseMainSliderPositionComponent {
    constructor(parent) {
        super(parent, "red");
    }
}
import { Character } from "./../Character.js";

/** This Humble Bundle image created by VisuStella. */
export class Cogsworth extends Character {
    constructor(context, x, y) {
        super(context, "CareerCharacterPortraits3", x, y, 0, 3);
    }
}
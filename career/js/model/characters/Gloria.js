import { Character } from "./../Character.js";

/** This Humble Bundle image created by Dreams Circle. */
export class Gloria extends Character {
    constructor(context, x, y) {
        super(context, "CareerCharacterPortraits1", x, y, 1, 3);
    }
}
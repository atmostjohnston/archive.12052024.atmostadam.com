import { GameCache } from "../persistence/GameCache.js";

export class CanvasDecorator {
    /*
    constructor(canvas, ctx) {
        if (!CanvasDecorator.instance) {
            CanvasDecorator.instance = this;
        }
        GameCache.set("canvas", canvas);
        GameCache.set("ctx", ctx);
        GameCache.set("width", canvas.width);
        GameCache.set("height", canvas.height);
        this.canvas = canvas;
        this.ctx = ctx;
        return CanvasDecorator.instance;
    }
    */
}